# Dependencies for the Employee File Scanner web app only (used by run.bat's
# portable Python setup). The full requirements.txt also covers the Gmail CLI.
Flask==3.0.3
openpyxl==3.1.5
pdfplumber==0.11.0
python-dotenv==1.0.1
anthropic>=0.39.0
certifi
